sudo: required

services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

before_install:
  - if [ ! -d "$HOME/google-cloud-sdk/bin" ]; then rm -rf $HOME/google-cloud-sdk; curl https://sdk.cloud.google.com | bash; fi
  # Add gcloud to $PATH
  - source /home/travis/google-cloud-sdk/path.bash.inc
  - gcloud version
  - gcloud --quiet components update kubectl
  # Auth flow
  - echo $GCLOUD_KEY | base64 --decode > gcloud.json
  - cat gcloud.json
  - gcloud auth activate-service-account $GCLOUD_EMAIL --key-file gcloud.json

cache:
  directories:
    - "$HOME/google-cloud-sdk/"
after_success:
  - docker build -t gcr.io/$CLOUDSDK_CORE_PROJECT/$CLOUDSDK_CORE_PROJECT .
  - gcloud docker push gcr.io/$CLOUDSDK_CORE_PROJECT/$CLOUDSDK_CORE_PROJECT > /dev/null